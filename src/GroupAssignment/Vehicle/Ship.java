package Vehicle;
import Vehicle.Vehicle;

public class Ship extends Vehicle {

    public Ship(String id, String name, double carryingCapacity, double currentFuel, double fuelCapacity, String currentPort) {
        super(id, name, carryingCapacity, currentFuel, fuelCapacity, currentPort);
    }
}
